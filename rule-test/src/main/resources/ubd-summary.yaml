UBI_EVENTS_SUMMAEY: |
    select * from ubi_bot_dq_overall

UBI_SESSIONS_SUMMARY: |
    select * from ubi_session_dq_overall

UBI_SESSIONS_EVENTS_SUMMARY: |
    select * from ubi_session_events_rt_batch_summary

UBI_SESSIONS_BOT_FLAG_SUMMARY: |
    select
    prod_botFlag
    ,translate(rt_BotFlagList, ' ', '')
    ,unsync_percent
    from dq_bot_flag_summary
    order by unsync_percent desc

UBI_SESSION_SAMPLE: |
    select * from ubi_session_rt limit 9