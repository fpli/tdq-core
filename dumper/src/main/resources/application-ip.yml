kafka:
  consumer:
    topic:
      - behavior.totalv2.signature.ip

flink:
  app:
    name: Sojourner RNO Ip Signature HDFS Load Pipeline
    parallelism:
      source: 10
      sink:
        hdfs: 10

hdfs:
  dump:
    path: hdfs://apollo-rno/sys/soj/ubd/signatures/type=ip
    class-name: com.ebay.sojourner.common.model.BotSignature