flink:
  app:
    name: "test123"
    profile: test
    sink:
      normal-metric:
        hdfs-path: target/${flink.app.profile}/metric/normal
      latency-metric:
        std-name: latency
      sample-log:
        std-name: sample
      exception-log:
        std-name: exception