kafka:
  consumer:
    topic:
      - behavior.totalv2.signature.agent

flink:
  app:
    name: Sojourner RNO Agent Signature HDFS Load Pipeline
    parallelism:
      source: 10
      sink:
        hdfs: 10

hdfs:
  dump:
    path: hdfs://apollo-rno/sys/soj/ubd/signatures/type=agent
    class-name: com.ebay.sojourner.common.model.BotSignature